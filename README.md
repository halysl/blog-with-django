# Blog with Django

一个基于 **Django** 框架开发的个人博客系统。这是一个学习 Django 项目结构和开发流程的练手项目，创建于 2018 年左右。

## ✨ 功能特性

### 博客核心功能
- 📝 **文章管理** - 支持使用 Markdown 格式撰写和展示文章
- 🏷️ **分类系统** - 为文章添加分类，便于内容组织
- 🔖 **标签系统** - 灵活的标签功能，支持多标签关联
- 📅 **归档功能** - 按年月归档文章，方便历史内容浏览
- 💬 **评论系统** - 内置评论功能，支持访客留言互动

### 技术特点
- 🐍 使用 **Python 3.7** + **Django 2.2.3** 开发
- 📖 支持 **Markdown** 语法，带代码高亮和目录生成
- 🗄️ 使用 **SQLite** 作为数据库，轻量易部署
- 🎨 基于 **Bootstrap 3** 的响应式前端设计
- 🔒 内置 Django Admin 后台管理系统

## 📁 项目结构

```
blog-with-django/
├── blog/                   # 博客主应用
│   ├── models.py           # 数据模型 (Post, Category, Tag)
│   ├── views.py            # 视图函数
│   ├── urls.py             # URL 路由配置
│   ├── admin.py            # 后台管理配置
│   ├── templatetags/       # 自定义模板标签
│   └── static/             # 静态资源文件
├── comments/               # 评论应用
│   ├── models.py           # 评论数据模型
│   ├── views.py            # 评论视图
│   ├── forms.py            # 评论表单
│   └── urls.py             # 评论路由
├── firstblog/              # Django 项目配置
│   ├── settings.py         # 项目设置
│   ├── urls.py             # 主 URL 配置
│   └── wsgi.py             # WSGI 配置
├── templates/              # 模板文件
│   ├── base.html           # 基础模板
│   ├── blog/               # 博客相关模板
│   └── comments/           # 评论相关模板
├── manage.py               # Django 管理脚本
├── Pipfile                 # Pipenv 依赖配置
└── Pipfile.lock            # Pipenv 锁定文件
```

## 🛠️ 技术栈

| 类型        | 技术                          |
| ----------- | ----------------------------- |
| 后端框架    | Django 2.2.3                  |
| Python 版本 | Python 3.7                    |
| 数据库      | SQLite                        |
| Markdown    | Python-Markdown               |
| 前端框架    | Bootstrap 3                   |
| 代码高亮    | highlight.js                  |
| 包管理      | Pipenv                        |

## 🚀 快速开始

### 环境要求
- Python 3.7+
- Pipenv

### 安装步骤

1. **克隆项目**
   ```bash
   git clone <repository-url>
   cd blog-with-django
   ```

2. **安装依赖**
   ```bash
   pipenv install
   ```

3. **激活虚拟环境**
   ```bash
   pipenv shell
   ```

4. **数据库迁移**
   ```bash
   python manage.py migrate
   ```

5. **创建超级用户** (用于访问后台管理)
   ```bash
   python manage.py createsuperuser
   ```

6. **启动开发服务器**
   ```bash
   python manage.py runserver
   ```

7. **访问应用**
   - 博客首页: http://127.0.0.1:8000/
   - 后台管理: http://127.0.0.1:8000/admin/

## 📋 URL 路由

| URL                               | 功能         |
| --------------------------------- | ------------ |
| `/`                               | 博客首页     |
| `/posts/<id>`                     | 文章详情页   |
| `/archives/<year>/<month>/`       | 文章归档     |
| `/categories/<id>/`               | 分类文章列表 |
| `/tags/<id>/`                     | 标签文章列表 |
| `/comment/post/<post_id>/`        | 发表评论     |
| `/admin/`                         | 后台管理     |

## 📦 数据模型

### Post (文章)
- `title` - 文章标题
- `body` - 文章内容 (Markdown 格式)
- `excerpt` - 文章摘要 (自动生成)
- `created_time` - 创建时间
- `modified_time` - 修改时间
- `category` - 所属分类
- `tags` - 标签 (多对多)
- `author` - 作者

### Category (分类)
- `name` - 分类名称

### Tag (标签)
- `name` - 标签名称

### Comment (评论)
- `name` - 评论者名字
- `email` - 邮箱
- `url` - 网址 (可选)
- `text` - 评论内容
- `created_time` - 评论时间
- `post` - 关联文章

## 🎨 前端特性

- 响应式设计，适配移动端和桌面端
- Black & White 简约主题风格
- Markdown 渲染支持
- 代码语法高亮 (highlight.js)
- 代码块行号显示
- 自动生成文章目录 (TOC)

## ⚠️ 注意事项

这是一个学习项目，主要用于熟悉 Django 框架的基本结构和开发模式。如需用于生产环境，建议：

1. 修改 `settings.py` 中的 `SECRET_KEY`
2. 将 `DEBUG` 设置为 `False`
3. 配置 `ALLOWED_HOSTS`
4. 考虑使用 PostgreSQL 或 MySQL 替代 SQLite
5. 配置静态文件服务

## 📄 License

本项目仅供学习参考使用。

---

> 🕐 Created around 2018 as a Django learning project.
